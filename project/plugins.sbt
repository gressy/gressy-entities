// The Play plugin. Added so we can compile the routes file.
addSbtPlugin("com.typesafe.play" % "sbt-plugin" % "2.5.3")

// For simpler releases to Maven Central
addSbtPlugin("org.xerial.sbt" % "sbt-sonatype" % "1.1")
